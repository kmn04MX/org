import "./Header.css"

function Header(){//debe haber un elemento que envuelve a más elementos

    return <header className="header">
    <img src='/img/Header.png' alt="ORG"/>
    </header>;
}

export default Header;